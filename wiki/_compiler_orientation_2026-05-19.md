# Compiler Orientation - 2026-05-19

System review note for future compiler sessions. This is not evidence for
Shinzen claims; load target content pages and their cited source pages for
domain claims.

## Basis

This pass read `wiki/index.md`, recent `wiki/log.md`, `wiki/Current Model.md`,
`wiki/_operations.md`, `commands/review.md`, `wiki/_page_catalog.md`,
`wiki/_yt_lecture_ingest.md`, the active `@ShinzenVideos` plan surfaces,
`wiki/_post_ingest_knowledge_health_plan.md`, the 2026-05-16 router
benchmark, and the main guidance, safety, accountability, and Source routers.
It also ran `tools\wiki_lint.cmd`.

Current mechanical baseline after the later 2026-05-19 closeout pass:

- Lint passes: 343 compiled pages and 313 raw sources checked.
- Page mix: 267 source pages, 59 concept pages, 9 syntheses, 7 questions, and
  1 thesis.
- Status mix: 334 working pages and 9 mature pages.
- Confidence mix: 263 established, 71 probable, 8 speculative, and 1
  contested.
- Raw backlog diagnostic is intentional: 46 canonical raw sources remain
  without source pages, dominated by query-driven non-channel items,
  series-candidate, audit-needed, and query-driven `@ShinzenVideos` items.

## Current Read

The compiler is no longer source-starved. It has a real `[[Current Model]]`,
strong first-load routing from `wiki/index.md`, mature live-guidance and
safety routers, a catalog layer, and a selective source-ingest policy.

The main constraint is now context engineering: future agents need fewer
tokens loaded before making good practice, safety, service, and transmission
judgments. The wiki's accumulated evidence is strong enough that adding more
sources is usually lower ROI than improving retrieval, maturity, and
frontmatter discipline.

The earlier active exception is now closed: `aqwdLfL6u48`, `[[Say What You
Mean]]`, was ingested and integrated, and the selected `@ShinzenVideos`
promoted queue is complete through Gate 39. Channel ingest should remain
paused unless a concrete query, audit-needed duplicate/source-frame check, or
deliberately promoted series cluster reopens it.

## What The Compiler Needs

1. **Keep source-next subordinate to system-next.** The index now correctly
   names no active promoted source gate. Global compiler ROI is compiled
   knowledge health: routing, compression, maturity, and drift detection.

2. **Protect mature routers from ingest drift.** The latest frontmatter
   pruning cleared current oversized-source diagnostics, but future ingests
   can still turn routing cards back into hidden bibliographies. Keep
   frontmatter bounded on first-load or mature pages such as
   `[[Practice Guidance Toolkit]]`, `[[Practice Cycles]]`, `[[Nurture Positive]]`,
   `[[Total Happiness]]`, and high-risk service or guidance pages.

3. **Continue route-tested maturity passes.** The best Phase 8 candidates are
   high-load working pages whose first 30 to 60 lines should function as
   decision surfaces: `[[Noting]]`, `[[See Hear Feel]]`,
   `[[Complete Experience]]`, `[[Insight and Purification]]`, `[[Flow]]`,
   `[[Gone]]`, `[[No-Self And Personality]]`, `[[Lineage Translation]]`, and
   `[[Shinzen's Teaching Method]]`. Promote only after route tests, not because
   the page is important.

4. **Add health tooling before more broad refactor.** Phase 9 lint advisories
   would catch drift earlier: high-importance large pages, mature pages with
   source-heavy frontmatter, missing decision maps on long hubs, excessive
   `best_linked_pages`, long `load_when`, and safety-risk terms without focused
   boundary links.

5. **Run a fresh router benchmark before the next major refactor.** The
   2026-05-16 benchmark has already paid off. A new benchmark should include
   teaching precision, public correction, accountability, behavior mismatch,
   Source/service overclaim, no-self/DPDR, pain/intensity, method choice, and
   source provenance.

6. **Decompose only when a route test proves load cost.** The biggest pages are
   not automatically the worst pages. `[[Current Model]]` has a useful card;
   `[[Practice Guidance Toolkit]]` is large but routable. Split or compress
   pages when the benchmark shows sibling loading, source chronology, or
   duplicated mechanism prose is wasting context.

## Highest-ROI Next Steps

1. Keep the recurring `@ShinzenVideos` ingest loop paused unless query,
   audit, or series evidence deliberately reopens it.
2. Run one small route-surface cleanup batch on high-load routers, especially
   where chronology or long source lists obscure first-pass decisions.
3. Run a fresh router benchmark and record the result as a system note.
4. Add Phase 9 advisory checks to `tools/wiki_lint.py`, keeping them advisory
   rather than hard failures.
5. Continue Phase 8 maturity passes one page at a time, beginning with the
   highest-load core practice pages that still lack mature route-tested
   surfaces.

## Lower-ROI Or Not-Now Moves

- Do not wholesale ingest the remaining `defer-query-driven` corpus.
- Do not treat `IntroToUltra` or the duplicate welcome transcript as active
  queue items unless a query makes them relevant.
- Do not create generic Buddhism, neuroscience, clinical, or comparative
  religion pages unless they change Shinzen-specific routing.
- Do not split a large page merely because it is large.
- Do not promote Source, science, AI, ultrasound, metaphysics, or service
  claims beyond their current calibrated evidence tier.
